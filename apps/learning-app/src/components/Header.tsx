import { ConnectButton } from "./ConnectButton";

export const Header = () => {
  return (
    <header className="flex p-4 justify-end w-full mx-auto max-w-screen-lg">
      <ConnectButton />
    </header>
  );
};
